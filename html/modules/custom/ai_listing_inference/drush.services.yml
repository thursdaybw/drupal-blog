services:
  ai_listing_inference.process_command:
    class: Drupal\ai_listing_inference\Command\AiListingProcessCommand
    arguments:
      - '@ai_listing_inference.ready_inference_runner'
    tags:
      - { name: drush.command }
