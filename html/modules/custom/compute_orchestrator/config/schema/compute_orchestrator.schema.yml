compute_orchestrator.settings:
  type: config_object
  label: 'Compute Orchestrator settings'
  mapping:
    vast_api_key:
      type: string
      label: 'Vast API key'
    max_hourly_price:
      type: float
      label: "Maximum hourly Vast price"

